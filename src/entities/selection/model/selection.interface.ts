export interface ISelectionDigits {
  title: string;
  digit: string;
}

export interface ISelectionTBodyItem {
  date: string;
  people: string;
  awards: string;
}

export interface ISelectionTextList {
  title: string;
  list: string[];
}
